[project]
name = "viviian"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
"numpy",
"psutil",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["viviian*"]

[tool.unittest]
start-dir = "tests"
pattern = "test_*.py"
